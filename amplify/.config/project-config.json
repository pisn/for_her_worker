{
    "projectName": "forher",
    "version": "3.0",
    "frontend": "javascript",
    "javascript": {
        "framework": "ionic",
        "config": {
            "SourceDir": "src",
            "DistributionDir": "www",
            "BuildCommand": "npm run-script build",
            "StartCommand": "ionic cordova run android --device --no-native-run --consolelogs"
        }
    },
    "providers": [
        "awscloudformation"
    ]
}